# Git Secrets - เอกสารประกอบ

เครื่องมือป้องกันการ commit secrets (รหัสผ่าน, API keys, tokens) โดยไม่ได้ตั้งใจ

## 🔒 สิ่งที่ถูกตรวจสอบ

- API Keys (AWS, GitHub, GitLab, etc.)
- Private Keys (RSA, DSA, EC, OpenSSH)
- Database Connection Strings
- Passwords & Secrets
- JWT Tokens
- Access Tokens

## 📁 ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | รายละเอียด |
|------|-----------|
| `.git/hooks/pre-commit` | Hook ที่ตรวจสอบก่อน commit |
| `.git-secrets-patterns` | รูปแบบ (patterns) ที่ต้องการตรวจสอบ |
| `.git-secrets-allowed` | รายการที่อนุญาตให้ผ่าน |

## 🚀 วิธีใช้งาน

### สำหรับผู้พัฒนา

**Commit ปกติ:**
```bash
git add .
git commit -m "feature: add new feature"
```
ถ้ามี secrets จะถูก block อัตโนมัติ

**ถ้าต้องการ bypass (ใช้เฉพาะกรณีที่แน่ใจ):**
```bash
git commit --no-verify -m "fix: update config"
```

### สำหรับ Admin

**ตรวจสอบทั้ง repository:**
```bash
# Windows (Git Bash)
sh .git/hooks/pre-commit

# Linux/Mac
.git/hooks/pre-commit
```

**เพิ่ม patterns ใหม่:**
แก้ไขไฟล์ `.git-secrets-patterns`

**เพิ่มรายการยกเว้น:**
แก้ไขไฟล์ `.git-secrets-allowed`

## ⚠️ กรณีที่ถูก Block

### 1. AWS Credentials
```php
// ไม่ควร commit แบบนี้
aws_access_key_id = "AKIAIOSFODNN7EXAMPLE"
aws_secret_access_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
```

### 2. Database Password
```php
// ไม่ควร commit รหัสผ่านจริง
database.default.password = "real_password_123"
```

### 3. Private Keys
```
-----BEGIN RSA PRIVATE KEY-----
```

## ✅ วิธีที่ถูกต้อง

### ใช้ Environment Variables
```php
// .env
DB_PASSWORD=your_password_here

// Config/Database.php
'password' => getenv('DB_PASSWORD') ?: '',
```

### ใช้ Placeholder
```php
'password' => 'your_password_here', // เปลี่ยนใน production
```

## 🔧 การตั้งค่าเพิ่มเติม

### ติดตั้งใน CI/CD

**GitHub Actions:**
```yaml
- name: Scan for secrets
  run: |
    git secrets --scan
```

### อัพเดท patterns

```bash
# เพิ่ม pattern ใหม่
sh -c 'echo "your_pattern_here" >> .git-secrets-patterns'
```

## 🐛 แก้ปัญหา

### ปัญหา: Commit ถูก block แต่ไม่ใช่ secrets

**วิธีแก้:**
1. เพิ่มใน `.git-secrets-allowed`:
   ```
   your_safe_string_here
   ```

2. หรือใช้ `--no-verify` (ใช้เฉพาะกรณีที่แน่ใจ):
   ```bash
   git commit --no-verify -m "your message"
   ```

### ปัญหา: Hook ไม่ทำงาน

**ตรวจสอบ:**
```bash
# Windows
ls -la .git/hooks/

# ต้องมีไฟล์ pre-commit (ไม่มี .sample)
```

**แก้ไข:**
```bash
cp .git/hooks/pre-commit.sample .git/hooks/pre-commit
# หรือรัน setup script
```

## 📋 Checklist ก่อน Commit

- [ ] ไม่มี API keys จริงในโค้ด
- [ ] ไม่มีรหัสผ่านจริงในโค้ด
- [ ] ไม่มี private keys
- [ ] ใช้ environment variables สำหรับ secrets
- [ ] ตรวจสอบว่า hook ทำงาน (commit จะถูก block ถ้าพบ secrets)

## 📞 ติดต่อ

หากพบปัญหาหรือต้องการเพิ่ม patterns ใหม่ ติดต่อทีม DevOps
