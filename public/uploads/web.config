<?xml version="1.0" encoding="UTF-8"?>
<!--
  โฟลเดอร์นี้เก็บเฉพาะไฟล์อัปโหลด (static: รูป/เอกสาร) ห้ามรันสคริปต์
  requestFiltering ปฏิเสธ request ไปยังนามสกุลที่รันได้ → กัน RCE หากมีไฟล์สคริปต์หลุดเข้ามา
  (ใช้ requestFiltering เพราะ IIS delegation = Allow; ไม่ใช้ <handlers> ที่ถูกล็อก Deny → จะทำให้ทั้งโฟลเดอร์ 500)
  หมายเหตุ: allowUnlisted เป็น attribute ของ <fileExtensions> เท่านั้น (ใส่ผิดที่ = 500)
-->
<configuration>
    <system.webServer>
        <security>
            <requestFiltering>
                <fileExtensions allowUnlisted="true">
                    <add fileExtension=".php" allowed="false" />
                    <add fileExtension=".php3" allowed="false" />
                    <add fileExtension=".php4" allowed="false" />
                    <add fileExtension=".php5" allowed="false" />
                    <add fileExtension=".php7" allowed="false" />
                    <add fileExtension=".phtml" allowed="false" />
                    <add fileExtension=".phar" allowed="false" />
                    <add fileExtension=".asp" allowed="false" />
                    <add fileExtension=".aspx" allowed="false" />
                    <add fileExtension=".ashx" allowed="false" />
                    <add fileExtension=".asmx" allowed="false" />
                </fileExtensions>
            </requestFiltering>
        </security>
    </system.webServer>
</configuration>
